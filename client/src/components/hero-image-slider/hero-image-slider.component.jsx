import React from "react";
import { HeroImageSliderContainer } from "./hero-image-slider.styles";

// 1. Set of images 6
const mockData_REPLACE_LATER = [
  {
    title: "1",
    genre: "1",
    rating: "1",
  },
];
// 2. Every 3 seconds transition the images
// 3. Add left/right toggles to skips to next images
// 4. Display title, genre, rating, more info

export const HeroImageSlider = () => {
  return <HeroImageSliderContainer>HeroImageSlider</HeroImageSliderContainer>;
};
